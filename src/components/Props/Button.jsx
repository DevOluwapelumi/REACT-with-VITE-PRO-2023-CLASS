

const Button = (props) => {
    console.log(props);
    return (
        <div>
            {/* <button className="{props.style}">{props.name}</button> */}
        </div>
    );
};

export default Button;